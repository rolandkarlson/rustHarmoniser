[package]
name = "rustnote"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
once_cell = "1.18" # useful for lazy static config if needed

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
