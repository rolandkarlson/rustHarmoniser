[package]
name = "rustnote"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
fxhash = "0.2"
rayon = "1.8"
dashmap = "5.5"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

